import torch
import torchvision
from torch.utils.data import Dataset, DataLoader
import numpy as np
import math

class WineDataset(Dataset):
    def __init__(self):
        # data loading
    pass

    def __getitem__(self, index):
        # dataset[0]
        pass

    def __len__(self):
        # len(dataset)
        pass